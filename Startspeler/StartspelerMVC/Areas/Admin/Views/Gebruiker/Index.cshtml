
<h1 class="bg-info text-white">Alle Gebruikers</h1>
<a asp-action="Create" class="btn btn-secondary">Voeg een gebruiker toe</a>

<table class="table table-sm table-bordered table-bordered">
    <tr><th>Alias</th><th>Naam</th><th>Email</th><th>Drankkaarten</th><th>Geboortedatum</th></tr>
    @foreach (var user in Model)
    {
        <tr>
            <td class="badge-light">@user.UserName</td>
            <td>@user.Voornaam @user.Achternaam</td>
            <td>@user.Email</td>
            <td>
                <ul>
                    @foreach (var kaart in user.Drankkaarten)
                    {
                        
                       
                        if (kaart.Status == "Openstaand")
                        {


                            if (kaart.Aantal_beschikbaar == 0)
                            {
                    <li class="text-danger">

                        id: @kaart.DrankkaartID / @kaart.DrankkaartType.Grootte <br />
                        beschikbaar: @kaart.Aantal_beschikbaar
                    </li>
                            <li>@kaart</li>
                        }
                        else
                        {
                <li>
                    id: @kaart.DrankkaartID<br />
                    beschikbaar: @kaart.Aantal_beschikbaar / @kaart.DrankkaartType.Grootte <br />
                </li>
                        }
                    }
                }
                </ul>
            </td>
            <td>@user.Geboortedatum.</td>
            <td><a class="btn btn-sm btn-primary" asp-action="Update" asp-route-id="@user.Id">Wijzig</a></td>
            <td>
                <form asp-action="Delete" asp-route-id="@user.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">
                        Verwijder
                    </button>
                </form>
            </td>
        </tr>
    }
</table>
