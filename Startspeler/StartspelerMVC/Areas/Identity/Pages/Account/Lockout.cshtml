@page
@model LockoutModel
@{
    ViewData["Title"] = "op slot.";
}

<header>
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">Dit account is op slot, contact een verantwoordelijke.</p>
</header>
